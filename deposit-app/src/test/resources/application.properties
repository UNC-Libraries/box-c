project.version=4.0

fedora.repository.name=Carolina Digital Repository
fcrepo.baseUrl=http://localhost:48085/fcrepo/rest
fcrepo.client.host=localhost:48085
fcrepo.auth.host=localhost
fcrepo.auth.user=fedoraAdmin
fcrepo.auth.password=fedoraAdmin

redis.host=localhost
redis.port=46380

# Java Message Server
jms.broker.uri=tcp://localhost:46161
cdr.stream=activemq:queue:repository.events
jms.deposit.job.stream=activemq:queue:deposit.job.queue
jms.deposit.operation.stream=activemq:queue:deposit.operation.queue
jms.deposit.pipeline.stream=activemq:queue:deposit.pipeline.queue

# SMTP Mail Server
smtp.host=localhost
smtp.port=1025

# ClamAV Service
clamd.host=localhost
clamd.port=3310
clamd.timeout=300000
clamd.maxStreamSize=128000000

# Email Addresses (admin at yourrepository.org, no-reply at yourrepository.org)
administrator.email=no-reply@localhost
repository.from.email=no-reply@localhost

deposits.dir=
deposits.tdb.dir=

workers.num=2
workers.high.num=2
workers.low.num=1

job.bagitValidation.workers=8
job.fileValidation.workers=1
job.fileValidation.maxQueuedJobs=5
job.fileValidation.flushRate=100
job.fixityCheck.flushRate=100
job.extractTechnicalMetadata.workers=3
job.extractTechnicalMetadata.maxQueuedJobs=5
job.extractTechnicalMetadata.flushRate=5
job.transferBinaries.workers=3
job.transferBinaries.maxQueuedJobs=5
job.transferBinaries.flushRate=100
fits.http.client.socketTimeout=0
fits.maxFileSizeForWebService=0

baseUrl=https://localhost:8080/
cleanup.delay.seconds=604800
status.keys.expire.seconds=604800

fits.baseUri=
fits.homePath=

derivative.dir=

cache.contentPath.maxSize=500
cache.contentPath.timeToLive=5000

cache.objectAcls.maxSize=100
cache.objectAcls.timeToLive=5000

fuseki.baseUri=http://localhost:43030/fuseki/fcrepo

ingestSources.path=
ingestSourceMappings.path=
ingest.upload.staging.path=

storageLocations.path=
storageLocationMappings.path=
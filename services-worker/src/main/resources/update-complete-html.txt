<html>
<head>
  <style>
    span {
      color: darkred;
    }
  </style>
</head>
<body>
  {{^issues}}
    <h3>CDR Metadata update completed </h3>
  {{/issues}}
  {{#issues}}
    <h3>CDR Metadata update completed with issues: {{fileName}}</h3>
  {{/issues}}
  <p>The XML metadata update you submitted has completed.  {{updatedCount}} object(s) were updated{{#outdatedCount}}, {{outdatedCount}} object(s) updates were out of date{{/outdatedCount}}{{#skippedCount}}, {{skippedCount}} object(s) were skipped because they were not changed{{/skippedCount}}{{#failedCount}}, {{failedCount}} object(s) failed to update due to errors{{/failedCount}}.</p>
  
  <p>If you require any assistance with this update, please reply to this email.</p>

  {{#outdatedCount}}
    <h4>Out of date documents ({{outdatedCount}})</h4>
    <p>Updates were not applied because the version in the repository has a more recent than the imported document.</p>
    <ul>
      {{#outdated}}
      <li>{{this}}</li>
      {{/outdated}}
    </ul>
  {{/outdatedCount}}

  {{#failedCount}}
    <h4>Failed to update ({{failedCount}})</h4>
    <p>Could not be updated due to errors</p>
    <ul>
      {{#failed}}
      <li>{{key}} - <span>{{value}}</span></li>
      {{/failed}}
    </ul>
  {{/failedCount}}

  {{#updatedCount}}
    <h4>Updated ({{updatedCount}})</h4>
    <p>Successfully updated</p>
    <ul>
      {{#updated}}
      <li>{{this}}</li>
      {{/updated}}
    </ul>
  {{/updatedCount}}

  {{#skippedCount}}
    <h4>Skipped updates ({{skippedCount}})</h4>
    <p>The following submitted documents were the same as the repository copy, so no update was performed.</p>
    <ul>
      {{#skipped}}
      <li>{{this}}</li>
      {{/skipped}}
    </ul>
  {{/skippedCount}}
</body>
</html>
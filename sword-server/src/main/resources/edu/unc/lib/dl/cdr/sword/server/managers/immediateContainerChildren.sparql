PREFIX fedora-model: <info:fedora/fedora-system:def/model#>
PREFIX cdr-base: <http://cdr.unc.edu/definitions/1.0/base-model.xml#> 
SELECT ?pid ?slug
FROM <%1$s>
WHERE
{
	<%2$s> cdr-base:contains ?pid .
	?pid fedora-model:label ?slug .
	?pid fedora-model:hasModel <%3$s> .
}